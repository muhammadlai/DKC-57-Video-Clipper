# DKC 57 Video Clipper — Frontend Configuration
# Copy this file to .env.local and fill in your values

# Backend API URL
NEXT_PUBLIC_API_URL=http://localhost:8000

# Optional: API key sent to the backend (only needed when the backend
# runs with D57_API_KEY set)
# NEXT_PUBLIC_API_KEY=change-me
